# Node modules (all dependencies, huge files, never commit!)
node_modules/

# Expo local files (auto-generated, machine-specific)
.expo/
.expo-shared/

# System files (macOS/Windows cruft)
.DS_Store
Thumbs.db

# Log files (debugging, errors)
*.log
npm-debug.log*

# Environment files (for secrets, if you use them)
.env
